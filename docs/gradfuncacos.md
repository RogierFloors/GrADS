---
title: acos
---

# **acos**

`acos(`*`expr`*`)`

Applies the cos<sup>-1</sup> function to the result of *`expr`*. Values from *`expr`* that exceed 1 or are less than -1 are set to missing. The result of the `acos` function is in radians.

## Usage Notes

### Examples
