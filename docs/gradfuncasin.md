---
title: asin
---

# **asin**

`asin(`*`expr`*`)`

Applies the sin<sup>-1</sup> function to the result of *`expr`*. Values of *`expr`* that exceed 1 or are less than -1 are set to missing in the final result. The result of the `asin` function is in radians.

## Usage Notes

### Examples
